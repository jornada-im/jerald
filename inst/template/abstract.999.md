This is an example dataset for testing \`jerald\` with an LTER Metabase instance using dataset 999. Data here come from R's \`mtcars\` example dataset.

You can replace this abstract with one for your dataset.
